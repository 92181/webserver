# Compact C Web Server
Performant and minimal web server, capable of providing full-duplex communication via websockets. This web server template has built-in support for the websocket protocol. This small server only has one dependency, the openssl library, it also uses some system libraries, wich are included by default.

# Usage
Yet to write;
